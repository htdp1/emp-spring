server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  config:
    import: 
    - application-mariadb.yml
  application:
    name: emp-spring-local
  data:
    rest: 
       base-path: "/"

logging:
  level:
    '[com.htdp1.emp.empspring]': debug